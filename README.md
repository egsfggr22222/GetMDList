# GetMDList
把项目文件夹变成一份 MD，直接发给 AI。 不用一个个复制代码，不用配置 agent 读文件夹。 拖入 → 生成 → 拖出去。完事。  One-click: turn your project into a single MD for any AI. No copying files one by one. No agent setup. Just drag, generate, drop.
